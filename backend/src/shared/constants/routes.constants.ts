export const ROUTES = {
  GITHUB: {
    SYNC: '/github/sync',
    RECENT: '/github/recent',
  },
  METRICS: {
    SUMMARY: '/metrics/summary',
    TRENDS: '/metrics/trends',
    LEADERBOARD: '/metrics/leaderboard',
  },
} as const;
